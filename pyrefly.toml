# pyrefly.toml

project_includes = ["pandas"]

# untyped-def-behavior = "check-and-infer-return-any"

site_package_path = [
  ".venv/lib/python3.12/site-packages/"
]

[errors]
# Todo support union of None
internal-error = false
# Todo support overrides
not-a-type = false
bad-override = false
# Todo fix type alias
type-alias-error = false
